{
  "name": "weather-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack MERN weather dashboard using OpenWeatherMap.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm start -w server",
    "build": "npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
